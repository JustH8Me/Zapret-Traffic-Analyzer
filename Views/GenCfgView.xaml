<UserControl x:Class="ZapretTraficAnalyz.Views.GenCfgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#F0F0F0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Финальный список (list-general.txt):" DockPanel.Dock="Top" Margin="0,0,0,5"
                       FontWeight="Bold" />
            <TextBox Text="{Binding ResultList}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Padding="5" FontFamily="Consolas" />
        </DockPanel>

        <StackPanel Grid.Column="1" Margin="0,10,10,10">
            <Border Background="White" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Настройки BAT" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" />

                    <TextBlock Text="Стратегия (Args):" />
                    <TextBox Text="{Binding Strategy}" Height="60" TextWrapping="Wrap" Margin="0,5,0,15" Padding="5" />

                    <TextBlock Text="Путь к zapret (winws.exe):" />
                    <TextBox Text="C:\zapret\winws.exe" Margin="0,5,0,15" Padding="5" />

                    <CheckBox Content="Запускать от Админа" IsChecked="True" Margin="0,0,0,20" />

                    <Button Command="{Binding CreateBatFileCommand}"
                            Content="СОЗДАТЬ ФАЙЛЫ"
                            Height="40" Background="#28a745" Foreground="White" FontWeight="Bold" Cursor="Hand" />
                </StackPanel>
            </Border>

            <TextBlock Text="Подсказка:" Margin="0,20,0,5" FontWeight="Bold" />
            <TextBlock Text="Скопируйте полученный .bat и .txt в папку с zapret и запустите bat файл."
                       TextWrapping="Wrap" Foreground="Gray" />
        </StackPanel>
    </Grid>
</UserControl>